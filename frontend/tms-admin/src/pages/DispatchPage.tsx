import PlaceholderPage from '../components/PlaceholderPage';

export default function DispatchPage() {
  return <PlaceholderPage title="배차계획" desc="배차 목록·상세·배송 CRUD·엑셀 업/다운로드·배차확정 화면 예정 (GET /dispatch …)" />;
}
